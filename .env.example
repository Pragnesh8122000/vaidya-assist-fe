VITE_API_URL=https://vaidya-assist-be.onrender.com/api
# SEC-15: VITE_AGENT_API_URL MUST be set in production to the deployed
# agent-service origin. Left empty here on purpose — a localhost default
# would silently break the chatbot in prod if forgotten. Set e.g.
# VITE_AGENT_API_URL=https://agent.your-domain.com/api
VITE_AGENT_API_URL=
